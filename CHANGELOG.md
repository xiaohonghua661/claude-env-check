# 更新日志（Changelog）

## v1.3.0 - 2026-08-03

**时区改为跟随出口 IP + 新增「出口判时区一致」（根据实际情况修正）**

### 修复
- 实测本订阅所有美国家宽节点（美国 10-13）出口均在加州（America/Los_Angeles），**没有任何 EST/纽约节点**——旧版强制「判纽约」永远 FAIL，脱离实际
- 全面检测改为**时区自适应**：目标 = 出口 IP 时区，判定「系统时区 == 出口IP时区」一致性
- 原「出口判纽约 X/Y 库」改为「出口判时区一致」（显示出口时区 vs 系统时区）
- 「修复为英文+纽约」改为「修复为英文+美国」：修复时自动查出口 IP 时区并设置系统时区匹配（当前=太平洋时区）
- 浏览器「实时时区」检查同步改为与出口时区一致性

### 验收
- PowerShell 段语法解析 0 错误
- 逻辑单元测试：系统=NY+出口=LA → FAIL（不一致）；系统=PST+出口=LA → PASS（修复后一致）；出口=LA → 修复设 Pacific Standard Time
- 节点实测：美国10=172.56.179.16(LA) / 11=75.226.42.107(Sunnyvale) / 12=12.219.11.71(SF) / 13=12.19.137.13(SF)，全 America/Los_Angeles
## v1.2.1 - 2026-08-03

**修复：移除 UTF-8 BOM（cmd 无法运行的 bug）**

### 修复
- 移除 .bat 文件开头的 UTF-8 BOM。实测带 BOM 的 .bat 双击运行会报 `'@echo' 不是内部或外部命令`，无法启动。
- 文件现为 UTF-8 **无 BOM** + CRLF（cmd 正常解析；中文显示依赖脚本首行 chcp 65001）。

### 说明
- 记事本打开 .bat 若显示乱码，是 Windows 记事本对「UTF-8 无 BOM」文件的经典误读（按 ANSI/GBK 解码），属正常现象，**不是文件损坏**。请用 VS Code / Notepad++ 等 UTF-8 编辑器打开查看。
- 不要给 .bat 加 BOM，也不要另存为 ANSI/GBK，否则会导致 cmd 无法运行或中文乱码。

### 验收
- GitHub raw 首 3 字节 = 40 65 63（无 BOM）；cmd 黑盒实测可正常解析、中文回显正常。
## v1.2.0 - 2026-08-03

**修复：切换语言不再清空输入法（搜狗被删 bug）+ 新增微软拼音开关**

### 修复
- 「修复为英文+纽约」「切回中文+北京」不再用 Set-WinUserLanguageList 整体重建语言列表（旧版会清空 zh-Hans-CN 的输入法，导致**搜狗输入法从语言列表消失、打不出中文**）
- 改为**保留式重排**：只调整首位语言（en-US ⇄ zh-Hans-CN），逐语言保留原有全部输入法 TIP，zh-Hans-CN 缺搜狗时自动补回；默认输入法固定为美国键盘（0409:00000409）
- 切回中文时保留微软拼音开关的当前状态（不会把已关闭的微软拼音静默加回）

### 新增
- 「微软拼音开关」按钮：一键开启/关闭 zh-Hans-CN 中的微软拼音（**搜狗输入法始终保留**，不受影响），无需管理员权限，带错误兜底提示

### 验收
- PowerShell 段语法解析 0 错误（按工具自身 ReadAllLines+LF 提取方式）
- 逻辑单元验证：fixen 保留 [en-US(0409), zh-Hans-CN(搜狗+微软拼音)]；开关仅增删微软拼音 TIP、搜狗保留；fixzh 保留开关状态
- 黑盒验收：fresh subagent 3 段（阅读理解一致 / 语法 0 错+关键内容齐全 / 疑问 2 项已修复）
- 关联问题：搜狗输入法在重启后正常（引擎初始化不再被联网卡死）

## v1.1.2 - 2026-08-02

**文档：使用者声明 + 风险检测结论**

### 新增
- README 新增「使用者声明」：维护者是在美国生活的中国用户，环境配置对应其在美国的日常使用环境
- 记录 fuck-claude.com 风险检测结论：20/100（低风险），为 Windows 平台理论下限（字体 +18 / Emoji +2 为结构性信号）

### 验收
- 本地与 GitHub 内容一致；风险分两轮独立会话实测稳定 20/100
## v1.1.1 - 2026-08-02

**修复「区域格式」假 FAIL + 命令行中文加固**

### 修复
- 修复「区域格式」误报 FAIL：检测改读注册表 LocaleName（新进程的真实来源），不再受网页服务进程启动时缓存的旧区域格式影响
- 「修复为英文+纽约」增加 LocaleName 注册表兜底写入
- 命令行中文修复扩展到 HKLM 控制台默认设置（覆盖快捷方式/其他用户场景）
- 输出文件（铁证报告）始终生成在 bat 所在目录（活路径/相对路径），可随意移动工具位置

### 验收
- 黑盒验收：真实提权运行「修复为英文+纽约」（FIX_OK）→ 全新进程复核 14 项全部 PASS → /api/check 核心 PASS 0 FAIL（仅出口 IP 库分歧 WARN）
- 在新路径 C:\01_TEMP\Claude环境搭建 部署后复测：0 FAIL，铁证报告生成于 bat 旁

## v1.1.0 - 2026-08-02

**修复命令行中文乱码 + 修复提权脚本 bug**

### 新增
- 「修复命令行中文」按钮：一键把控制台默认代码页设为 65001（UTF-8）+ 中文字体 NSimSun，新开 cmd / PowerShell 可正常输入显示中文（不影响 en-US 伪装）
- 全面检测新增「命令行中文」检查项（代码页 + 字体）
- 「修复为英文+纽约」自动同步命令行中文修复

### 修复
- 修复提权修复脚本中注册表路径丢失反斜杠的 bug（HKLM:\SOFTWARE\Policies\Google\Chrome、Chrome User Data 等）

### 验收
- 黑盒验收：PASS（首页 / /api/fixcmd / /api/claude / /api/check 全部 200，命令行中文检查项 PASS）

## v1.0.0 - 2026-08-02

**首个公开版本（单文件网页版）**

### 新增
- 本地网页控制台 UI（浏览器操作界面，替代命令行菜单）
- 全面检测：系统/浏览器/网络分层证据，含 headless Chrome 实测浏览器指纹
- 铁证报告：检测结果自动存档（时间戳 txt）
- WebRTC 防泄露策略（WebRTCIPHandlingPolicy=disable_non_proxied_udp）
- Claude Code 客户端环境检查（时区/区域/代理/WebRTC 策略）
- 一键修复为 英文+纽约 / 一键切回 中文+北京（含 Chrome 语言、WebRTC 策略）

### 修复
- 修复管理员权限下浏览器指纹无法获取的问题（检测自动降权 / 明确提示）
- 修复 headless Chrome 输出捕获竞态（重试读取 + 临时文件加载）
- DNS 改为 1.1.1.1/8.8.8.8（去除中国公共 DNS 指纹）

### 验收
- 黑盒验收测试：PASS（首页 UI / /api/check / /api/claude / 提权修复逻辑复测复现）